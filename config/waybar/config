{
    "layer": "top",
    "position": "top",
    "mod": "dock",
    "exclusive": true,
    "passthrough": false,
    "gtk-layer-shell": true,
    "height": 40,
    "margin-top": 10,
    "margin-left": 10,
    "margin-right": 10,

    "modules-left": ["custom/launcher", "group/hardware"],
    "modules-center": ["hyprland/workspaces", "wlr/taskbar"],
    "modules-right": ["group/utils", "custom/power"],

    // --- GRUPO HARDWARE (Esquerda) ---
    "group/hardware": {
        "orientation": "horizontal",
        "modules": ["cpu", "memory", "disk"]
    },

    // --- GRUPO UTILS (Direita - Agora com Rede) ---
    "group/utils": {
        "orientation": "horizontal",
        "modules": ["network", "pulseaudio", "clock"]
    },

    // --- MÓDULOS ---

    "custom/launcher": {
        "format": "  ",
        "on-click": "wofi --show drun",
        "tooltip": false
    },

    "hyprland/workspaces": {
        "disable-scroll": true,
        "all-outputs": true,
        "on-click": "activate",
        "persistent_workspaces": { "1": [], "2": [], "3": [], "4": [] }
    },

    // --- NOVO: Ícones dos Apps Abertos ---
    "wlr/taskbar": {
        "format": "{icon}",
        "icon-size": 18,
        "tooltip-format": "{title}",
        "on-click": "activate",
        "ignore-list": ["wofi"]
    },

    "cpu": {
        "interval": 10,
        "format": " {usage}%",
        "max-length": 10
    },

    "memory": {
        "interval": 30,
        "format": " {}%",
        "max-length": 10
    },

    "disk": {
        "interval": 30,
        "format": " {percentage_used}%",
        "path": "/"
    },

    // --- NOVO: Rede Ethernet ---
    "network": {
        "format-ethernet": "", 
        "format-disconnected": "⚠",
        "tooltip-format": "{ipaddr}",
        "on-click": "kitty --title 'nmtui' nmtui"
    },

    "clock": {
        "format": "{:%H:%M}",
        "tooltip-format": "{:%A, %d %B %Y}"
    },

    "pulseaudio": {
        "format": "{icon} {volume}%",
        "format-muted": "",
        "on-click": "pavucontrol",
        "format-icons": {
            "default": ["", "", ""]
        }
    },

    "custom/power": {
        "format": "",
        "tooltip": false,
        "on-click": "wlogout"
    }
}
